<h2 class="title is-1">Новый курс</h2>
<%= render 'form' %>
<% flash.each do |type, msg| %>
  <div>
    <%= msg %>
  </div>
<% end %>
<div>
  <%= button_to 'Вернуться', courses_path, method: :get %>
</div>
